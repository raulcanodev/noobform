import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['res.cloudinary.com', 'fakeimg.pl', 'lh3.googleusercontent.com', 'img.youtube.com'],
  },
};

export default nextConfig;
